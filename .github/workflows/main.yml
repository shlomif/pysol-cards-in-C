# This file is GENERATED BY
# docker_ci.py
jobs:
    hello_world_job:
        name: A job to test docker
        runs-on: ubuntu-latest
        steps:
        -   uses: actions/checkout@v2
            with:
                submodules: 'true'
        -   name: docker test action step
            uses: ./my-gh-actions-ci
'on':
- push
