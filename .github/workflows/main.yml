# This file is GENERATED BY
# docker_ci.py
jobs:
    hello_world_job:
        name: A job to test docker
        runs-on: ubuntu-latest
        steps:
        -   id: hellodocker
            name: docker test action step
            uses: shlomif/pysol-cards-in-c@master
'on':
- push
